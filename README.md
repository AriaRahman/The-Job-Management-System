
<h1 align="center">Employment DB — Java + MySQL</h1>

## 📌 Overview
This project demonstrates a **Java (JDBC)** program that connects to a **MySQL** database named `employment`, reads rows from the `store` table, and prints **Job Account No, Job Name, Salary, Authority ID**.  
It also includes a full **database schema** for an employment scenario with tables for **authority**, **employee**, **store**, **gather**, **require**, and **assign**, along with sample data and example SQL queries (single-row, aggregate, subqueries, and views).

---

<h2>🧰 Tech Stack</h2>

- **Language:** Java 8+  
- **Database:** MySQL 8.x  
- **Driver:** MySQL Connector/J (e.g., `mysql-connector-j-8.x.x.jar`)  
- **Build/Run:** `javac` / `java` (or any IDE)

---
>
